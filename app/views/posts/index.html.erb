<section class="input-area">
	<% if Current.user %>
		<%= render 'posts/form' %>
	<% else %>
		<%= render 'posts/signin' %>
	<% end %>
</section>

<section id="timeline">
	<%= render partial: 'posts', locals:  {posts: @posts} %>
</section>